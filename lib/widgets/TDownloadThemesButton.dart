import 'package:flutter/material.dart';

class TDownloadThemesButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      tooltip: 'Download Themes',
      child: Icon(Icons.save_alt),
    );
  }
}
